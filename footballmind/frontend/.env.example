# Copy to .env (gitignored). Point at the Render backend URL, no trailing slash,
# e.g. VITE_API_BASE=https://footballmind.onrender.com
# Leave empty to run the UI on built-in demo data.
# NOTE: VITE_* vars are baked into the public JS bundle -- never put secrets here.
VITE_API_BASE=
